# Frontend README

This is the frontend for Agentic Commerce (React + Vite + TypeScript + Tailwind).

Local dev:

1. cd frontend
2. npm install
3. copy .env.example to .env.local and edit if needed
4. npm run dev

Build:

npm run build
npm run preview
